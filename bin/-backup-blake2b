#!/usr/bin/env python3
import hashlib
import sys
blake2b = hashlib.blake2b()
while True:
    data = sys.stdin.buffer.read(1024 ** 2)
    if not data:
        break
    blake2b.update(data)
blake2b = blake2b.hexdigest()
print(blake2b)
